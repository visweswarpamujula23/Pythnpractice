Python supports file handling and allows users to handle the files to read and write files
it supports many other file handling options

















Dictionary consists of multiple elements but in terms of (key value) pair we call them as mappings rather than

divide the schedule into 5 parts

